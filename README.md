# “Stellar Burgers”

Реализация `vite` + `react` + `redux` + `ts`

## Как запустить

В директории с проектом запустите установку зависимостей:

### `npm install`

Запустите проект:

### `npm run dev`
